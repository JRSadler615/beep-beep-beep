# Supabase project (Database / Auth via GoTrue)
VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-key"

# FastAPI backend base URL.
# Leave empty to use same-origin relative /api paths (recommended in
# production behind Cloudflare). In local dev, vite.config.ts proxies /api
# to VITE_API_PROXY (default http://localhost:8000).
#
# PROD HOSTNAME: if the deployed SPA and API are on different origins, set
# this to the backend's public URL (e.g. https://api.jrsadler.com). Leave
# empty only if a reverse proxy serves the API under the SPA's own origin.
VITE_API_URL=""

# Dev-only: where Vite proxies /api requests (the local FastAPI server).
# PROD HOSTNAME: dev tunnel/backend only — not used in production builds.
VITE_API_PROXY="http://localhost:8000"
