/**
 * Shared frontend constants. Mirrors the backend's `app/constants.py` — keep
 * the two in sync.
 */

/**
 * Default eBay "condition description" (seller note) shown before the user's
 * saved value loads, and used as a fallback. Worded media-agnostically because
 * the app lists DVDs, CDs, VHS, and cassettes.
 */
export const DEFAULT_SELLER_NOTE =
  "Please note: any mention of a digital copy or code may be expired and/or " +
  "unavailable. This does not affect the quality or functionality of the product."
